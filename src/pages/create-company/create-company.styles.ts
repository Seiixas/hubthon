import { styled } from "styled-components";

export const StyledCreateCompanyWrapper = styled.main`
  border-radius: 10px;
  align-items: center;
  text-align: justify;
  padding: 2.5rem;
  text-justify: inter-word;
  max-width: 700px;

  display: flex;
  flex-direction: column;
  gap: 1rem;

  p {
    font-size: 1rem;
  }

  box-shadow: 0px 0px 68px -24px rgba(0, 0, 0, 1);
`;
